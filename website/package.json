{
    "name": "fylo-website",
    "version": "26.25.1",
    "private": true,
    "packageManager": "bun@1.3.11",
    "scripts": {
        "start": "bun run serve",
        "serve": "bun run bundle && ty preview",
        "bundle": "ty bundle && bun --no-env-file ../scripts/patch-tachyon-runtime.mjs . && bun --no-env-file ../scripts/fingerprint-assets.mjs",
        "preview": "bun run bundle && ty preview"
    }
}
