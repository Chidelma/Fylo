/**
 * The browser TTID module is a bundled artifact with no ambient type package.
 * Mirror the shapes FYLO relies on here.
 *
 * @typedef {string} TTID
 * @typedef {{ createdAt: number, updatedAt?: number, deletedAt?: number }} TTIDTimestamps
 */

export {}
